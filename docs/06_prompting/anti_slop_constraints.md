# Anti-Slop Constraints

When generating imagery, include constraints to prevent the model from producing random decorative marks that do not belong to Oraphine. Suggested phrases:

- avoid random decorative squiggles
- avoid human letters
- avoid obvious runes
- avoid fake Arabic
- avoid hieroglyph knockoffs
- avoid logo-like icons
- avoid inconsistent symbol families
